services:
  mysql:
    image: mysql:8.0
    container_name: tradeiq-mysql
    restart: unless-stopped
    environment:
      MYSQL_ALLOW_EMPTY_PASSWORD: "yes"
      MYSQL_DATABASE: tradeiq
    ports:
      - "3307:3306"
    volumes:
      - mysql_data:/var/lib/mysql
      - ./backend/migrations/schema.sql:/docker-entrypoint-initdb.d/schema.sql

  backend:
    build: ./backend
    container_name: tradeiq-backend
    restart: unless-stopped
    env_file: ./backend/.env
    environment:
      FLASK_APP: run.py
      DB_HOST: mysql
      DB_PORT: 3306
      DB_PASSWORD: ""
    depends_on:
      - mysql
    ports:
      - "5000:5000"

  frontend:
    build:
      context: "./frontend/DRA App"
      dockerfile: Dockerfile
    container_name: tradeiq-frontend
    restart: unless-stopped
    environment:
      EXPO_PUBLIC_API_URL: "https://trade-iq-deploy.onrender.com"
    ports:
      - "8082:8081"
    depends_on:
      - backend

volumes:
  mysql_data:

