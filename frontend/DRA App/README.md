
  # Investment Banking Challenge App

  ## Running the code

  Run `npm i` to install the dependencies.

  Run `npm run dev` to start the development server.

  Run `npx expo start` to view project frontend from scanning QR code in Expo Go
  
